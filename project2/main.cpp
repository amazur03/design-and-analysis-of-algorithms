#include "Upload.hpp"

int main()
{
    int size = 100000; // Specify the size of the ranking array
    Upload test(size); // Create an instance of the Upload class with the specified size

    test.show(); // Display the ranking data

    return 0;
}
